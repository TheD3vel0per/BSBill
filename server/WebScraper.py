class WebScraper:

    def __init__(self):
        pass

    def get_bill_text_from_url(self, url):
        """ # Get the page contents from url
        if isinstance(url, str):
            page = requests.get(url).text
            self.soupHTML = BeautifulSoup(page, 'html.parser').prettify()
            self.soupText = self.soupHTML.get_text()
        else:
            print("URL not a string")"""
        pass
